{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd apps/api && npm install && npx prisma generate --schema=prisma/schema.prisma && npx tsc && ls dist/"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/server.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
