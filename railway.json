{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd apps/api && npx prisma generate && npx tsc"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/server.js",
    "healthcheckPath": "/health"
  }
}
